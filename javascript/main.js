let monthlyRent = 500;

monthlyRent = 1;

const yearlyRent = monthlyRent * 12;
console.log(yearlyRent);

// let'de değişken sabit değildir. kendi scope'u dışında işlev görmez.
// var ile aralarındaki var ise var'ın bütün her yerde geçerli olması
// let'in ise kendi scope'u içerisinde geçerli olmasıdır.

//const monthlyRent = 500;

//monthlyRent = 1;

//const yearlyRent = monthlyRent * 12;
//console.log(yearlyRent);

//hata verir. const'da değişken sabittir.
